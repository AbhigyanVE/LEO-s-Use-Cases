For Now, Only InfoExtractor is on Development Phase, Rest all are just PRESENT


URL
 ↓
Playwright → full HTML
 ↓
Rule-based extractor → price, specs, images, features
 ↓
HF NER → model names, brand hints
 ↓
Field completeness check
 ↓
LLM called ONLY if fields missing
